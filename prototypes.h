/*Laura Stalker
LS451494
lstalker@albany.edu


Function protoypes for text to binary/ binary to text file conversion program
*/

/*A function that converts a text file to a binary file
*/
void textToBinary( FILE* inPtr, FILE* outPtr);

/*A function that converts a binary file to a text file
*/
void binaryToText( FILE* inPtr, FILE* outPtr);

/*A function that prints info about the contents of a binary file
*/
void binaryInfo( FILE* infile);
